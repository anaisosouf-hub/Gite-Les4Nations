server {
    listen 8080;
    root /home/site/wwwroot;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Support for .html files without extension
    location ~ ^/([^.]+)$ {
        try_files $uri $uri.html $uri/ =404;
    }
}
